# Node environment
NODE_ENV=production

# The app database is a single SQLite file (see db/init-sqlite.js); no
# connection string or credentials needed. In Docker it's mounted at
# /data/MDD_Candy.db via the fete_data volume (docker-compose.yml).
